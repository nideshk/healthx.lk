"use client";

import LanguageToggle from "@/components/common/LanguageToggle";

export default function AppHeader() {
  return (
    <header className="w-full flex items-center justify-end px-6 py-3 border-b border-slate-100 bg-white sticky top-0 z-50">
    </header>
  );
}
